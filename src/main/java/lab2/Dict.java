package lab2;

import java.util.Arrays;
import java.util.List;

public class Dict {
    List<String> reserveWords = Arrays.asList(
            "begin", "end", "if", "then", "or", "for", "while", "and", "write", "read",
            "long", "int", "double", "string", "char", "real", "var"
    );
    List<String> separators = Arrays.asList(
            ";", ".", "," , "(", ")", ":=", "!=", "+", "-", "/", "*", ":"
    );

}
